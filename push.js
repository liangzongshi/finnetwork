const { getClient } = require('bottender')
const tlgClient = getClient('telegram')
const msgClient = getClient('messenger')

const send = async (platform, id, notiId, title, event_type, data, templates) => {
    if (platform == 'web'){

    }

    if (platform == 'msg'){

    }

    if (platform == 'tlg'){

    }
}